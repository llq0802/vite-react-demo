import React from 'react';
import styles from './cmp.less';

type PropsType = {};

const Cmp: React.FC<PropsType> = ({}) => {
  return <div className={styles.container}>Cmp</div>;
};

export default Cmp;
